---
interface Props {
  as?: string;
  class?: string;
}
const { as: Tag = 'article', class: klass = '' } = Astro.props;
---

<Tag
  class={`rounded-2xl border border-[color:var(--color-line)] bg-[color:var(--color-surface)] p-4 shadow-[0_1px_0_rgba(0,0,0,0.02)] ${klass}`}
>
  <slot />
</Tag>
